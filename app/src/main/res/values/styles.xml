<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Стиль для выбранной вкладки -->
    <style name="TabTextAppearanceSelected">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#FF0000</item> <!-- Красный цвет для выделенной вкладки -->
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Стиль для невыбранной вкладки -->
    <style name="TabTextAppearance">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#888888</item> <!-- Серый цвет для невыделенной вкладки -->
        <item name="android:textStyle">normal</item>
    </style>
    <style name="RoundedImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="BottomNavigationViewStyle" parent="Widget.Design.BottomNavigationView">
        <item name="itemRippleColor">@android:color/transparent</item>

    </style>
    <style name="CircularImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="TabLayoutStyle" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabRippleColor">@android:color/transparent</item>
        <item name="android:textSize">10sp</item>
        <item name="tabSelectedTextColor">@android:color/black</item>
        <item name="tabTextColor">@android:color/black</item>
    </style>
</resources>
